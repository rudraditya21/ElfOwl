# TODO 2026-03-26 (Code-Based Readiness Audit)

Audit date: March 26, 2026  
Scope: `README.md` + `docs/REQUIREMENT.md` vs current codebase state.

## Compliance Field Gaps (CIS Accuracy)
- `container.runtime` is not populated; affects CIS_4.9.1.
- `container.isolation_level` is not populated; affects CIS_4.9.2.
- `kubernetes.audit_logging_enabled` is not populated; affects CIS_5.5.1.
Impact: these controls currently evaluate with zero values and can behave like unknown == violation.

## Kubernetes Metadata Gaps
- Owner references remain TODO in `pkg/kubernetes/client.go` (`OwnerRef: nil`). README/requirements mention owner tracking.
- Node metadata retrieval exists (`GetNodeMetadata`) but is not wired into enrichment output.

## Runtime Telemetry Limitations
- Process argv/exe path is best-effort (`process.c` uses comm for argv; no full cmdline or exe path).
- DNS parsing is best-effort (simple question parsing only; no full EDNS or complex label compression support).
- Capability monitoring filters a subset of high-risk caps only (intentional, but document if wider coverage is required).

## Testing / Validation Gaps
- No E2E cluster tests under `test/e2e`.
- Evidence signing/encryption tests are missing.
- API client tests are missing.
- eBPF integration tests require privileged kernel context; ensure CI coverage or document manual steps.

## Documentation Mismatches
- `docs/REQUIREMENT.md` says “single-replica controller” but deployment is a DaemonSet.
- README referenced `docs/CIS_CONTROLS.md` and `CONTRIBUTING.md` which do not exist.
- README previously claimed owner reference tracking (still TODO).

## Suggested Next Steps
- Implement owner reference extraction and populate missing compliance fields.
- Add evidence/API client tests and E2E cluster validation.
- Align requirement and README docs with the DaemonSet deployment model.
